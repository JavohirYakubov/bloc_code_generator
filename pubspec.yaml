name: bloc_code_generator
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.4.3

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  path: ^1.8.0
  mustache_template: ^2.0.0  # For templating

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
